# Server Configuration
PORT=3000
NODE_ENV=production

# PostgreSQL Database Configuration
# These will be automatically provided by Easypanel
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_password_here
PGDATABASE=bookavan

# SendGrid Email Configuration
SENDGRID_API_KEY=your_sendgrid_api_key_here
FROM_EMAIL=noreply@yourcompany.com

# Microsoft Entra ID (Azure AD) Configuration
# Register your app at: https://portal.azure.com → Azure AD → App registrations

# Application (Client) ID from Azure Portal (REQUIRED for login to work)
ENTRA_CLIENT_ID=your-application-client-id-here

# Allowed Tenant IDs (optional - leave empty to allow all organizations)
# Comma-separated list. Get from Azure Portal → Azure AD → Overview
# Example: ENTRA_ALLOWED_TENANTS=abc123-def456-ghi789,xyz987-uvw654-rst321
# Leave empty or use 'organizations' to allow any organizational account
ENTRA_ALLOWED_TENANTS=

# Admin Configuration
# Comma-separated list of admin email addresses (full email addresses)
ADMIN_EMAILS=admin@company.com,it@company.com

